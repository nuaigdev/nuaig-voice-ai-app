// Product-level naming for this deployment. Anything that varies per community
// belongs in src/clients/<id>.ts instead.

export const PRODUCT = {
  name: 'Seabury Assistant',
  descriptor: 'Voice Console',
  tagline: 'The voice concierge for our community',
} as const;
